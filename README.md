# control_hora / ATLAS

Repositorio inicializado para versionar ATLAS.

La versión base será cargada en el siguiente commit con código limpio, SQL, manuales, prompt de construcción y estándar de versionado.
